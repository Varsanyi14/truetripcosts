--- truetripcosts-main/src/layouts/Base.astro	2026-08-06 21:31:20.000000000 +0000
+++ /home/claude/work/truetripcosts-main/src/layouts/Base.astro	2026-08-07 13:00:55.512651769 +0000
@@ -1,7 +1,10 @@
 ---
 import '../styles/global.css';
 import { countries } from '../data/index.js';
-const { title, description, ogImage } = Astro.props;
+// `mono` opts a page into IBM Plex Mono, used for the utility-desk labels on the
+// money rules cards. Off by default so the other 380-odd pages do not pay for a
+// font they never render.
+const { title, description, ogImage, mono = false } = Astro.props;
 const site = 'https://truetripcosts.com';
 const canonical = site + Astro.url.pathname;
 const ogImageUrl = site + (ogImage || '/og.png');
@@ -52,6 +55,7 @@
   <link rel="preconnect" href="https://fonts.googleapis.com">
   <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
   <link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,400;9..144,500;9..144,600&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
+  {mono && <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@500;600&display=swap" rel="stylesheet" />}
 </head>
 <body>
   <slot />
